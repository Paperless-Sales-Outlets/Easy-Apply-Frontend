# Overrides .env when running `npm run build` (Vite defaults to production mode).
# Keeps local dev (.env) pointed at localhost without risking a hosted build
# accidentally shipping the localhost URL again.

VITE_API_BASE_URL=https://dpdlab1.slt.lk:5050/Paperlessbackup/api
